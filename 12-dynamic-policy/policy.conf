# dynamic-policy config — one mute prefix per line, comments with #
# Paths listed here are muted (ES events suppressed) for AUTH_EXEC
# Edit this file at runtime; the monitor reloads it without restarting.
/private/var/folders
